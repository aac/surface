<!-- This project's agent + contributor guidance lives in AGENTS.md (vendor-neutral,
     the cross-tool convention). It is imported below so Claude Code auto-loads it at
     session start; everyone else — humans and other agents — reads AGENTS.md directly.
     Keep this file a thin pointer: content belongs in AGENTS.md, not here. -->
@AGENTS.md
